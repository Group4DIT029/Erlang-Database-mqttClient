%%%-------------------------------------------------------------------
%% @doc dbclient public API
%% @end
%%%-------------------------------------------------------------------

-module(dbclient_app).

-behaviour(application).

%% Application callbacks
-export([start/0, stop/1]).

%%====================================================================
%% API
%%====================================================================

start() -> {ok, Pid} = mysql:start_link([{host, "localhost"}, {user, "user"}, {password, "password"}, {database, "database"}]).
  

%%--------------------------------------------------------------------
stop(_State) ->
    ok.

%%====================================================================
%% Internal functions
%%====================================================================
